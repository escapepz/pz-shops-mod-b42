-- config/_index.lua
-- Configuration registry for ShopsHooksExample
-- Controls load order and which config files to include
-- Format: module path relative to config/ folder

return {
	"prices",
	"defaults",
}
